To Run Code:
    1, run this command in the root folder 
        npm install
    2, run this command also in the /reactapp folder
       folder
        npm install
    3, build the React app using at /reactapp
        npm run build
    4, run the following to start the server using
        npm start